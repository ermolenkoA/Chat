<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatRoom" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ChatRoom" parent="Base.Theme.ChatRoom" />

    <style name="ThemeOverlay.ChatRoom.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/smoky</item>
        <item name="fullscreenTextColor">@color/black</item>
    </style>
</resources>